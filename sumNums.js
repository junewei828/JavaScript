function sumNums(max) {
    var sum = 0;
    for (var i = 0; i <= max; i += 1) {
        sum = sum + i;
        }
        console.log(sum)
    }

sumNums(4);
sumNums(10);
sumNums(365);


